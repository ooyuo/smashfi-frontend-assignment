/** 가상 스크롤 아이템 높이 (px) */
export const VIRTUAL_ITEM_SIZE = 60;

/** 가상 스크롤 컨테이너 높이 (px) */
export const VIRTUAL_SCROLL_HEIGHT = 600;

/** 무한 스크롤 threshold (남은 아이템 개수) */
export const INFINITE_SCROLL_THRESHOLD = 20;

/** 가상 스크롤 overscan (화면 밖 렌더링 아이템 수) */
export const VIRTUAL_SCROLL_OVERSCAN = 5;

/** 페이지당 아이템 수 */
export const ITEMS_PER_PAGE = 100;

/** 현재 페이지 기준 유지할 페이지 범위 (앞뒤로 N페이지) */
export const PAGE_CACHE_RANGE = 2;
